# GraphQL Posts — React + TypeScript

Project sederhana untuk assessment: konsumsi API GraphQL publik (https://graphqlzero.almansi.me/api)  
Fitur: list posts (pagination), post detail (author + comments), create post (mutation).

## Tech Stack
- React (Create React App) + TypeScript
- graphql-request (GraphQL client)
- react-router-dom

## Setup
1. Clone repo  
2. Install dependencies  
```bash
npm install
